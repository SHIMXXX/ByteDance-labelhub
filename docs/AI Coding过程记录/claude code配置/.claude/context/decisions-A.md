| 日期 | 决策 | 原因 | 影响范围 |
| --- | --- | --- | --- |
| 2026-06-09 | Labeler Workbench 差异视图采用左右双栏对比布局，并精简 Header 高度 | 在标注重提场景下，垂直堆叠的差异展示会造成严重的视觉断层和滚动负担；改为双栏对比并收缩 Header 空间，能最大化核心画布的垂直可视区域，提升高频修改的沉浸感 | `frontend/src/pages/labeler/workbench/LabelerWorkbenchPage.tsx`、`frontend/src/styles.css` |
| 2026-06-09 | 桌面分辨率适配优先采用“全局样式断点 + 少量页面 class 收口”的最小改动方案，不重做页面结构 | 当前目标是让项目在 1280×800 与 1920×1080 两档演示分辨率下稳定、顺眼；相比大规模重构 JSX 结构，优先收口 `body/content/workbench` 的宽度策略、关键三栏/双栏 grid 和局部粘性高度，能更稳地解决横向溢出与过窄留白问题 | `frontend/src/styles.css`、`frontend/src/pages/reviewer/reviews/ReviewerReviewsPage.tsx`、`frontend/src/pages/reviewer/dashboard/ReviewerDashboardPage.tsx`、`frontend/src/pages/owner/tasks/OwnerTaskDetailPage.tsx`、`frontend/src/pages/owner/reviews/OwnerReviewManagementPage.tsx` |
| 2026-06-08 | Labeler Workbench 的“修改对比”采用“上次提交版本 vs 当前本地答案”的字段级 diff，并把快捷键能力局部收口在页面内 | 当前目标是补足 AI reject / 人工打回后的定向修改讲述能力与真实工作台质感；复用现有 `SubmissionVersion`、`build_answer_diff` 与 Workbench 动作函数能以最小改动落地，无需引入复杂文本级 diff 或全局快捷键框架 | `backend/app/api/routes/workbench.py`、`backend/tests/test_workbench_api.py`、`frontend/src/pages/labeler/workbench/LabelerWorkbenchPage.tsx`、`frontend/src/pages/labeler/workbench/LabelerWorkbenchPage.test.tsx`、`frontend/src/types/domain.ts` |
| 2026-06-07 | 关键配置项引入“工作台 (Workspace)”模式，支持实时调试 (Test Run) 与双栏预览 | 复杂的 AI 预审、审核管理需要沉浸式操作空间，单页 Modal 已无法承载“修改-预览-测试”的循环；通过 Workspace 模式大幅提升专业度 | `frontend/src/pages/owner/tasks/AIConfigWorkspace.tsx`、`backend/app/api/routes/tasks.py` |
| 2026-06-07 | 新建任务逻辑采用三阶段向导 (Wizard) 并引入可视化日期滚动组件 | 任务创建涉及基础信息、数据模板绑定、AI 配置三个不同维度的决策，拆分向导可降低操作压力；可视化时间选择器能显著降低日期格式输入错误的概率 | `frontend/src/pages/owner/tasks/OwnerTasksPage.tsx` |
| 2026-06-07 | 模板 Designer 正式升级为“先绑数据集、双分区页面、schema v3”的样本驱动设计器 | 用户明确要求模板先围绕真实样本设计原始数据展示区与作答区，原有“空白表单编辑器”心智已不满足产品目标；但当前实现仍需尽量复用现有模板版本化接口、Renderer 与拖拽底座 | `backend/app/api/routes/templates.py`、`backend/app/models.py`、`frontend/src/pages/owner/templates/*`、`frontend/src/types/domain.ts`、`frontend/src/features/renderer/*` |
| 2026-06-06 | Owner 端本轮 UI 改版继续沿用现有 `AppFrame + styles.css + 页面单文件` 骨架，通过分级侧栏、卡片重组、弹窗入口与独立 Designer 外壳做稳态升级 | 当前目标是提升演示观感与产品感，而不是扩大到新 UI 框架或全局状态重构；小步升级更稳，也更贴合现有测试基线 | `frontend/src/router/roleMenus.ts`、`frontend/src/layouts/AppFrame.tsx`、`frontend/src/App.tsx`、`frontend/src/pages/owner/tasks/*`、`frontend/src/pages/owner/datasets/*`、`frontend/src/pages/owner/templates/*`、`frontend/src/pages/owner/exports/*` |
| 2026-05-21 | 模板搭建器第一版固定字段白名单，仅支持 `label/field/required` 及少量组件专属字段 | 防止低配设计器范围膨胀，降低新手实现复杂度 | 设计稿、实施计划、前端模板配置、后端 schema |
| 2026-05-21 | AI 预审结果固定映射为：`pass/human_review` 进入 Reviewer 待审，`reject` 进入待修改，Reviewer 拥有最终裁决权 | 提前消除前后端对状态流转理解不一致的问题 | 状态机、审核接口、前端状态展示 |
| 2026-05-21 | 导出按 `JSON → CSV` 优先实现，其余格式后置 | 降低实现压力，确保先交付稳定导出能力 | 导出模块、验收节奏 |
| 2026-05-21 | 第一版 audit log 只覆盖关键事件，不做字段级审计 | 控制实现范围，满足可追溯即可 | 审计日志模型、后端事件写入 |
| 2026-05-21 | 将登录/角色入口页壳子、导出历史页壳子等支撑类任务偏向成员 B，以减轻成员 A 负担 | 成员 A 任务过重，易成为前端瓶颈 | 分工策略、实施计划 |
| 2026-05-21 | 提交状态第一版简化为 `draft/submitted/ai_passed/needs_revision/review_passed`，不单独保留 `ai_rejected` 与 `review_rejected` | 避免状态定义与实际流转脱节，减少前后端判断分支 | 设计稿、接口契约、前后端状态处理 |
| 2026-05-21 | 第一版鉴权固定使用 Bearer token；AI trigger 接口不进入前端正常调用链路，仅供后端内部调试或补偿 | 降低前端实现歧义，避免重复触发 AI 审核 | 接口契约、前端请求封装、后端任务触发 |
| 2026-05-25 | 后续前后端整合开发统一在 `SM` 分支推进，`LWH` 仅保留为参考 / 归档分支 | 真实后端能力已并入 SM，需要统一默认工作入口并减少上下文分裂 | `CLAUDE.md`、`PLANROAD-A.md`、A 侧进度与决策记录 |
| 2026-06-03 | 阶段 3 的审核链路采用“`Submission` 当前态 + `SubmissionVersion` 历史层”，而不是每次重提新建一条 `Submission` | 需要兼容现有 assignment + dataset_item 唯一键、workbench 聚合口径、Owner 统计与导出口径，避免多题主链路整体重写 | `backend/app/models.py`、`submissions/reviews` 路由、Reviewer 页面、导出逻辑 |
| 2026-06-03 | 高级模板规则只做 `visibleWhen` 与 `validationRules` 的最小表达能力，并保持 schema v1/v2 双兼容 | 目标是严格收口阶段 3 验收，不引入复杂 DSL、跨层拖拽与后端规则引擎，降低实现和验收不确定性 | `templates/submissions` 路由、`frontend/src/features/renderer/*`、Owner 模板页 |
| 2026-06-03 | 阶段 3 收口继续沿用“当前态驱动 + 历史层追溯 + 审计时间线过滤”的 Reviewer 口径 | 当前重点是补齐测试、异常态与兼容展示，而不是再扩新状态机 | `test_submissions_api.py`、`test_reviews_api.py`、`test_workbench_api.py`、Reviewer 页面、Renderer |
| 2026-06-04 | 阶段五第一轮权限收口采用“显式 helper + 任务级 reviewer 分配”，不引入通用 ACL / RBAC 框架 | 当前目标是最小补齐三角色资源归属与 reviewer 任务级分配闭环，若直接引入通用权限框架会扩大改动面、增加验收不确定性 | `access_control`、`tasks/reviews` 路由、`OwnerTasksPage`、接口契约 |
| 2026-06-04 | 正式运行路径默认强制 Bearer JWT，`get_demo_user()` 仅在 `APP_ENV=test` 保留测试旁路 | 需要在不大改现有测试底座的前提下收口认证旁路，同时避免正式运行继续自动回退 demo 身份 | `backend/app/api/deps.py`、`backend/app/main.py`、`backend/tests/conftest.py`、README、接口契约 |
| 2026-06-04 | `/reviews/{submission_id}/assign` 继续保留 submission 级接口，但必须校验目标 reviewer 存在、角色正确且属于当前 task 的 reviewer assignment | 审查指出原接口缺少目标 reviewer 合法性校验，存在越权与脏分配风险 | `backend/app/api/routes/reviews.py`、`backend/tests/test_reviews_api.py` |
| 2026-06-04 | reviewer 批量审核接口复用单条 submission 访问控制，若 submission 已分配给其他 reviewer 则直接拒绝 | 审查发现 `/reviews/bulk/approve` 与 `/reviews/bulk/reject` 存在跨 reviewer 批量处理越权面 | `backend/app/services/access_control.py`、`backend/app/api/routes/reviews.py`、`backend/tests/test_reviews_api.py` |
| 2026-06-04 | template 更新与 active-version 读取统一收口到 owner 归属校验 | 审查发现模板 PATCH 与 active-version GET 未强制 owner 归属，存在越权修改和未授权读取 | `backend/app/api/routes/templates.py`、`backend/tests/test_templates_api.py` |
| 2026-06-04 | draft/save 与 submit 的模板版本口径统一以后端 task 当前有效模板版本为准，前端传入的 `templateVersionId` 不再作为可信来源 | 质量审查发现 Labeler 可通过清空或伪造 `templateVersionId` 跳过模板必填校验；收口到后端可堵住绕过面并统一校验语义 | `backend/app/api/routes/submissions.py`、`backend/tests/test_submissions_api.py` |
| 2026-06-04 | 任务发布前置校验收口到“dataset + 至少 1 个 dataset item + template + active template version”四条件 | 空壳 task 仍可被发布并进入 plaza/workbench；相比在领取或作答时兜底，发布前 fail fast 更符合当前产品边界 | `backend/app/api/routes/tasks.py`、`backend/tests/test_tasks_api.py` |
| 2026-06-04 | 前端 401 统一采用“清 session + 派发 `auth-expired` 事件 + RoleGuard 回登录页”最小收口方案 | 当前项目仍是单页最小登录态，不值得引入完整全局状态管理；用 API client 与 RoleGuard 协作即可收住会话失效后的错误放大面 | `frontend/src/services/api/client.ts`、`frontend/src/router/guards.tsx`、`frontend/src/App.test.tsx` |
| 2026-06-04 | `tab_container` 运行态从“全部展开 children”升级为最小 Tab 切换，Designer 容器编辑继续保持“最小能力 + 小步补强”策略 | 当前阶段目标是补完题目核心缺口并保持低风险收口；先用最小 Tab 切换恢复真实交互语义，再在不重构的前提下逐步补 children 可视编辑、常用子组件入口与基础排序，避免一次性扩成完整高级 Designer 工程 | `frontend/src/features/renderer/Renderer.tsx`、`frontend/src/features/renderer/Renderer.test.tsx`、`frontend/src/pages/owner/templates/OwnerTemplatesPage.tsx`、`frontend/src/pages/owner/templates/OwnerTemplatesPage.test.tsx` |
| 2026-06-04 | 认证体验第二轮采用“退出登录 + 失效提示 + 用户名回填 + 多标签基础同步”收口，不引入 refresh token / remember me | 需要在不扩大到完整正式会话体系的前提下，把真实使用体验补到够用；用户名回填和多标签同步能明显改善日常联调与演示体验，但仍保持当前 Bearer JWT 简单边界 | `frontend/src/services/api/client.ts`、`frontend/src/router/guards.tsx`、`frontend/src/layouts/AppFrame.tsx`、`frontend/src/pages/auth/LoginPage.tsx`、`frontend/src/App.test.tsx`、`frontend/src/pages/auth/LoginPage.test.tsx` |
| 2026-06-04 | 保持 `PLANROAD-A.md` / `PLANROAD-B.md` 作为历史路线，另建 `PLANROAD-FINAL.md` 承接最终统一收口计划 | 当前已由同一人统一推进 A/B，最终阶段目标也从“继续补开发”转向“演示产品化与答辩交付”；若继续分别在 A/B 路线图中追加终局路线，会混淆历史开发路线与最终冲刺计划。单独总表更利于执行、对齐与交付 | `PLANROAD-FINAL.md`、`PLANROAD-A.md`、`PLANROAD-B.md`、`.claude/context/progress-A.md` |
| 2026-06-04 | 最终阶段默认降级安全深化与正式化治理，优先推进高观感、高体验、高演示收益的页面与交付项 | 用户已明确下一阶段目标是做出有设计感、体验好、功能完善、演示得通的产品；相较于 bcrypt / refresh token / 完整 RBAC，这些工作对最终答辩观感与讲述性提升更直接 | `PLANROAD-FINAL.md`、README、demo-script、用户手册、演示环境说明 |
| 2026-06-05 | P0 产品化阶段沿用现有 `AppFrame + RoleGuard + roleLayouts + styles.css` 骨架，小步补 Dashboard / 任务详情 / 工作台重排，而不引入新 UI 框架或全量组件库 | 当前代码已具备稳定主链路与较完整测试基线；若在 P0 同时重做路由壳层、状态体系和组件体系，风险会明显高于收益。用现有壳层承接产品化升级，更利于在保证可演示性的同时控制返工面 | `frontend/src/App.tsx`、`frontend/src/layouts/AppFrame.tsx`、`frontend/src/layouts/roleLayouts.tsx`、`frontend/src/styles.css` |
| 2026-06-05 | Labeler Dashboard 与我的贡献中心采用最小后端聚合接口 `/workbench/summary` 支撑，而不是由前端拼接现有广场 / 工作台接口 | 当前前端若只靠 plaza/workbench 现有接口很难稳定拿到“已领取任务 / 已提交 / 待修改 / 已通过 / 最近提交”这类聚合信息；新增最小 summary 接口可以保持边界清晰，并降低前端产品页实现复杂度 | `backend/app/api/routes/workbench.py`、`backend/tests/test_workbench_api.py`、`frontend/src/pages/labeler/dashboard/LabelerDashboardPage.tsx`、`frontend/src/pages/labeler/MyContributionPage.tsx` |
| 2026-06-05 | Reviewer P0 详情页继续沿用同页 list/detail 双态，不在本轮强拆独立详情路由 | 当前 `ReviewerReviewsPage` 已具备待审列表、批量操作与详情数据装载逻辑；先在同文件内强化信息结构和视觉层级，比新开详情路由更稳，也更符合本轮“以演示观感优先”的小步升级策略 | `frontend/src/pages/reviewer/reviews/ReviewerReviewsPage.tsx`、`frontend/src/pages/reviewer/reviews/ReviewerReviewsPage.test.tsx` |
| 2026-06-05 | 提交后的 submission 一旦进入 `submitted / ai_passed / review_passed`，前后端都不允许再回写为 `draft` | 质量审查指出 Labeler 提交成功后仍可能被 autosave 或手动“保存草稿”回滚成 draft，直接破坏主状态机与 Reviewer 待审链路；因此需要同时在前端清理 autosave/dirty 标记，并在后端 `save_draft` 路径增加状态保护 | `frontend/src/pages/labeler/workbench/LabelerWorkbenchPage.tsx`、`frontend/src/pages/labeler/workbench/LabelerWorkbenchPage.test.tsx`、`backend/app/api/routes/submissions.py`、`backend/tests/test_submissions_api.py` |
| 2026-06-05 | quota 在产品语义上继续表示“可领取人数上限”，任务广场与 claim 接口都按 `claimed_count < quota` 收口 | 质量审查指出当前前端已展示剩余额度，但后端仍按“单任务单领取”处理，容易在演示中穿帮；本轮选择保留 quota 语义，并通过移除 `Assignment.task_id` 单列唯一约束、统一 plaza 筛选与 claim 逻辑来闭环 | `backend/app/models.py`、`backend/app/api/routes/tasks.py`、`backend/tests/test_tasks_api.py`、`frontend/src/pages/labeler/plaza/LabelerPlazaPage.tsx` |
| 2026-06-05 | Owner/Labeler/Reviewer 的 P0 产品化页面继续沿用现有业务路由与局部详情模式，优先修复状态与语义问题而非再次扩信息架构 | P0 主体完成后，剩余问题主要集中在 Workbench 状态映射、reject reason 暴露条件、quota 语义与 create 页误导性按钮；此时继续扩新页面收益低，先把真实链路和当前 UI 语义对齐更稳 | `frontend/src/pages/labeler/workbench/LabelerWorkbenchPage.tsx`、`backend/app/api/routes/workbench.py`、`backend/app/api/routes/tasks.py`、`frontend/src/pages/owner/tasks/OwnerTasksPage.tsx` |
| 2026-06-05 | Owner 数据入口第一批采用“三段式完整页”（管理页 + 三步导入向导 + 详情/预览页），而不是继续嵌入任务页 | 需要把数据集从“任务绑定附属能力”升级为可独立演示的资产入口，同时避免继续放大 `OwnerTasksPage` 复杂度；现有 datasets API 已足够支撑最小产品化闭环 | `frontend/src/App.tsx`、`frontend/src/router/roleMenus.ts`、`frontend/src/services/api/datasets.ts`、`frontend/src/pages/owner/datasets/*`、`frontend/src/pages/owner/tasks/OwnerTasksPage.tsx` |
| 2026-06-05 | 第二批页面强化继续沿用现有业务路由 + 局部详情模式，以小迭代补中心页而不重建全局信息架构 | 当前 P1/P2 的高收益项主要是 Export Center、任务广场、审核队列与待修改中心的产品感强化；在不引入新框架的前提下，保持现有 `AppFrame + styles.css` 并逐页增强最稳 | `frontend/src/pages/owner/exports/OwnerExportsPage.tsx`、`frontend/src/pages/labeler/plaza/LabelerPlazaPage.tsx`、`frontend/src/pages/reviewer/reviews/ReviewerReviewsPage.tsx`、`frontend/src/pages/labeler/RevisionCenterPage.tsx` |
| 2026-06-06 | `llm_assist` 模板组件改为经后端代理调用真实 DeepSeek 服务，而不是继续保留前端本地 stub | 用户已明确要求按 `backend/.env` 配置接真实大模型；若前端直连第三方会暴露密钥，也不利于统一演示环境，因此本轮选择新增最小后端代理接口 `/workbench/llm-assist`，并保持现有模板组件/Workbench 交互壳层不变 | `backend/app/services/llm_assist.py`、`backend/app/api/routes/workbench.py`、`backend/tests/test_workbench_api.py`、`frontend/src/pages/labeler/workbench/LabelerWorkbenchPage.tsx`、`frontend/src/pages/labeler/workbench/LabelerWorkbenchPage.test.tsx`、`frontend/src/features/renderer/Renderer.tsx`、`README.md` |
| 2026-06-06 | `preference_compare` 专项流程补齐继续采用“现有通用链路增量扩展”的方案，不引入通用 taskType / ACL / 独立 FinalResult 表 | 目标是尽快补齐第十节真实流程缺口并形成可演示闭环；若同时抽象 taskType、权限系统或独立最终结果表，会显著扩大改动面并冲击当前稳定测试基线 | `Dataset/DatasetItem`、`Submission`、导入、模板、AI 审核、Reviewer、导出、前后端类型与测试 |
| 2026-06-06 | `preference_compare` 的题面展示采用轻量 `compare_panel`，而不是继续硬用 `show_item` 或直接引入通用字段可见性引擎 | `show_item` 无法自然表达 A/B 对比题面；但当前阶段也不值得为这一类题目引入复杂角色可见性 DSL。新增轻量组件可以最小成本提升真实工作台与审核页的可讲述性 | `templates` 路由白名单、`Renderer`、Designer 2.0、前端类型与相关测试 |
| 2026-06-06 | Reviewer“直接修订并通过”与“最终结果快照层”打包在同一轮实现，并把最终结果挂在 `Submission` 上 | 这两个能力天然绑定：如果 reviewer 可以改答案，但没有稳定快照，导出仍会回到 labeler 原答案。直接在 `Submission` 上补 `final_answer_json` 等字段，符合当前项目的小步补字段策略 | `backend/app/models.py`、`reviews` 路由、导出服务、Reviewer 详情页、测试 |
| 2026-06-06 | Owner 审核管理继续采用“独立监控页 + owner 聚合/列表接口”口径，而不是复用 reviewer 待审接口 | Owner 需要看任务级审核进度、reviewer 负载、AI 任务监控与最终提交结果，目标是全局可观测性而非代入 reviewer 权限视角；用 `/owner-reviews/*` 与 owner 侧 `/submissions` 独立收口更稳，也更利于页面讲述性 | `backend/app/api/routes/owner_reviews.py`、`backend/app/api/routes/submissions.py`、`frontend/src/pages/owner/reviews/*`、`frontend/src/router/roleMenus.ts` |

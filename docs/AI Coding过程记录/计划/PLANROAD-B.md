# Project 路线图

## 重要说明（2026-05-25）
- `LWH` 已转为参考 / 归档分支，后续开发统一由成员 A 在 `SM` 分支推进。
- 本文档继续保留成员 B 的原始职责、历史计划与阶段拆分，作为参考与回溯记录，不删除历史内容。
- B 侧后续以同步记录为主；`LWH` 中已有价值的后端能力会以并入 `SM` 后继续维护为准。

## 进度标记约定
- `[ ]` = 待开始
- `[-]` = 进行中 🏗️
- `[x]` = 已完成 ✅

## 成员 B 路线图（后台稳定器 Owner）

### 最近状态（同步说明）
- `LWH` 分支现定位为参考 / 归档分支，不再作为后续主要开发分支。
- B 侧已沉淀的后端骨架、接口契约与相关能力中，有价值部分已并入或正在并入 `SM` 分支统一维护。
- 后续此路线图以阶段计划为主，并持续同步关键状态，供 A 侧推进时参考。
- 登录、任务、模板、领取、workbench、草稿保存、提交等主链路真实接口已在 `SM` 分支完成联调；其中模板真实链路与带 schema 的完整标注链路已浏览器实测通过。

## 总目标
负责把系统底层能力和高风险模块做稳，并在当前 MVP 基础上逐步升级到更贴近题目验收的工程形态：
- 当前 MVP：登录、任务、模板、领取、草稿、提交、最小 AI 预审、单级 Reviewer 审核、JSON / CSV 导出、关键事件审计已打通
- 后续升级：补齐数据集导入、多题任务模型、工程化 AI Agent、多级人工审核、多格式异步导出、认证权限、部署与答辩材料

## 当前定位与差距摘要
### 当前已完成的 MVP 主链路
- [x] FastAPI + MySQL 核心骨架已完成，任务、模板、领取、草稿、提交等真实接口已联通
- [x] 最小 AI 预审闭环已完成，已具备 `scores` / `decision` / `summary` 持久化结果
- [x] Reviewer 最小审核闭环已完成，支持通过 / 打回与理由记录
- [x] JSON / CSV 导出已可生成真实内容
- [x] 审计日志第一版已覆盖关键状态迁移事件


## 第 1 周：后端基础能力与“提交前”接口（保留历史）

### 1. 项目基础搭建
- [x] 初始化 FastAPI 项目结构
- [x] 完成基础配置管理
- [x] 接入 MySQL
- [x] 建立基础运行说明

### 2. 核心数据模型
- [x] 建立 `users`
- [x] 建立 `tasks`
- [x] 建立 `templates`
- [x] 建立 `template_versions`
- [x] 建立 `assignments`
- [x] 建立 `submissions`
- [x] 约束最小状态机字段

### 3. 核心接口
- [x] 完成演示账号登录接口
- [x] 输出登录接口契约
- [x] 完成任务 CRUD 接口
- [x] 输出任务接口契约
- [x] 完成先到先得领取接口
- [x] 输出领取接口契约
- [x] 完成模板保存 / 读取接口
- [x] 输出模板 schema 契约
- [x] 完成草稿保存 / 提交接口
- [x] 输出提交接口契约

### 4. 支撑类任务（帮 A 减压）
- [x] 提供登录页 / 角色入口页所需最小接口支持
- [x] 提供前端初期需要的 mock 契约示例

### 5. 第 1 周交付物
- [x] 后端项目可启动
- [x] 数据库核心表可用
- [x] A 已拿到登录、任务、模板、提交 4 类核心契约
- [x] 提交前主链路接口可联调

---

## 第 2 周：AI 预审、人工审核、导出闭环（保留历史）

### 1. AI 预审基础模型与任务表
- [x] 建立 `ai_audit_jobs`
- [x] 建立 `ai_audit_results`
- [x] 接入 DeepSeek 模型
- [x] 补最小调用封装（当前为 `AIExecutor` / `DeepSeekAIExecutor` 边界，不是完整 `LLMClient`）
- [x] 固定结构化输出：`scores`、`decision`、`summary`

### 2. AI 结论映射（必须固定）
- [x] 固定 `pass` → Reviewer 待审
- [x] 固定 `reject` → 待修改
- [x] 固定 `human_review` → Reviewer 待审
- [x] 固定 Reviewer 拥有最终裁决权（当前为最小裁决记录）

### 3. AI 任务执行稳定性
- [x] 完成 AI 任务创建逻辑
- [x] 完成 AI 执行逻辑
- [x] 完成结果回写
- [x] 完成请求级超时控制
- [x] 完成重试计数落库（未实现自动重试）
- [x] 完成同步链路内失败转人工复核
- [x] 通过唯一约束保证一个 submission 仅持有一条 AI job 记录

### 4. 人工审核模块
- [x] 建立 `review_records`
- [x] 完成待审核列表接口
- [x] 完成审核详情接口
- [x] 完成通过接口
- [x] 完成打回接口
- [x] 打回要求理由必填

### 5. 导出模块
- [x] 建立 `export_jobs`
- [x] 导出顺序固定：JSON → CSV → JSONL → Excel
- [x] 先完成 JSON 导出
- [x] 再完成 CSV 导出
- [ ] 再完成 JSONL 导出
- [ ] 最后完成 Excel 导出
- [x] 若时间不足，优先保证 JSON / CSV 稳定
- [x] 完成导出任务创建、状态查询、真实内容生成接口（JSON / CSV 已可直接返回内容，未进入 JSONL / Excel）
- [x] 提供导出历史页所需最小接口与页面壳子支持

### 6. 审计日志
- [x] 建立 `audit_logs`
- [x] 第一版只记录关键事件：
  - 任务创建
  - 任务发布 / 暂停 / 结束
  - 任务领取
  - 提交答案
  - AI 预审完成
  - Reviewer 通过 / 打回
  - 导出创建 / 完成
- [x] 不做字段级审计

### 7. 第 2 周交付物
- [x] 提交后可触发 AI 预审
- [x] Reviewer 可处理审核结果
- [x] Owner 可发起导出
- [x] 黄金路径后端已完整打通
- [x] 测试环境已固定使用本地规则 executor，避免因默认 DeepSeek 配置导致第二周基线回归失真

---

## 后续阶段路线图（半重构版）

## 后续阶段 0：当前 MVP 后端稳定与修复
目标：让现有后端主链路稳定，不因模型返回、状态异常或演示数据缺失导致崩溃。

### 题目核心
- [x] 对 DeepSeek 返回结果增加 normalize，保证 `scores` 永远是数组
- [x] 增强 AI 输出 JSON schema 校验，避免脏结果直接进入 Reviewer 链路
- [x] AI 返回异常时写入可读错误信息，并自动转人工复核
- [x] 修复 JSONL / Excel 前后端能力不一致问题，继续以前后端一致能力为准

### 工程化增强
- [x] 补充 AI 调用日志，记录 provider、model、耗时、decision
- [x] 增加 seed demo 脚本，生成演示用户、模板、任务、数据项、提交、AI 结果、审核记录
- [x] 补充关键接口测试和状态机回归测试
- [x] 检查 `.env`、API Key、敏感配置不会被提交
- [x] 回归修复 AI / 审核 / 导出的高频问题



## 后续阶段 1：数据集与多题任务模型
目标：从当前最小任务模型升级为真实数据标注平台模型。

### 题目核心
- [x] 新增 `datasets` 表
- [x] 新增 `dataset_items` 表
- [x] 设计任务与数据集的绑定关系
- [x] 以现有 `datasets/datasets/qa_quality` 与 `datasets/datasets/preference_compare` 作为第一批真实导入样本，验证 JSON / JSONL / Excel 三种导入路径
- [x] 支持 JSON 导入数据集
- [x] 支持 JSONL 导入数据集
- [x] 支持 Excel 导入数据集
- [x] 支持数据项预览、分页、搜索
- [x] 调整 assignment / submission 模型，支持一个任务多条 item
- [x] 支持每个 item 独立草稿、提交、审核状态（当前核心接口、广场领取态回显、AI reject 待修改理由回传与真实浏览器闭环已具备）

### 工程化增强
- [x] 增加数据导入校验与错误报告结构
- [x] 补多题任务模型相关测试与迁移验证
- [x] 明确多题任务状态聚合口径，供前端任务进度展示复用

## 后续阶段 2：AI Agent 工程化
目标：从“同步调用模型”升级为“可配置、可追溯、可失败兜底的 AI 审核流水线”。

### 题目核心
- [x] 新增 AI 审核配置表（如 `ai_audit_configs`）
- [x] 支持任务级 Prompt 模板配置
- [x] 支持任务级评分维度配置
- [x] 支持通过阈值配置
- [x] 保存 AI 调用时使用的原始 prompt
- [x] 保存 AI 原始响应
- [x] 引入异步任务队列（当前已落地 Celery / Redis）
- [x] AI job 状态从 `queued` → `running` → `succeeded` / `fallback_human_review`
- [x] 实现失败重试
- [x] 实现幂等控制，避免同一 submission 重复生成多条 AI 审核
- [x] 增加 AI 审核结果结构化校验
- [x] 增加人工兜底路径

### 工程化增强
- [x] 抽清 `JobExecutor` / worker 执行边界，降低后续迁移与扩展成本
- [x] 补充 AI 审核相关测试、失败分类测试与审计断言
- [x] 优化 AI 审核结果结构，为后续多轮审核与导出复用做准备

## 后续阶段 3：多级人工审核与审计
目标：让审核流转符合题目中的初审 / 复审 / 终审和可追溯要求。

### 题目核心
- [x] 设计完整 submission 状态机
- [x] 新增审核阶段字段（如 `review_stage`）
- [x] 支持初审、复审、终审
- [x] 支持 Reviewer 分配
- [x] 支持批量通过
- [x] 支持批量打回
- [x] 支持打回理由必填
- [x] 支持 Labeler 修改后重新提交并生成新轮次
- [x] 保存多轮提交版本
- [x] 保存多轮审核记录
- [x] 支持答案 diff 所需的数据结构
- [x] 增强 `audit_logs`，记录每次状态迁移
- [x] 提供审核时间线接口

### 工程化增强
- [x] 增加状态机合法性测试
- [x] 增加批量审核、轮次变更、审计时间线接口测试
- [x] 统一 AI 审核、人工审核、提交版本之间的关联键设计

## 后续阶段 4：导出系统增强
目标：满足题目要求的多格式、异步、可配置导出。

### 题目核心
- [x] 支持 JSONL 导出
- [x] 支持 Excel 导出
- [x] 支持异步导出任务
- [ ] 支持导出进度状态
- [x] 支持导出文件下载
- [x] 支持导出历史列表
- [x] 支持字段映射配置
- [x] 支持导出时选择是否包含 AI 审核结果
- [x] 支持导出时选择是否包含人工审核记录
- [x] 支持仅导出 `review_passed` 数据

### 工程化增强
- [x] 增加导出格式测试
- [ ] 增加导出内容快照测试
- [x] 设计导出产物存储与清理策略

## 后续阶段 5：认证、权限与部署
目标：让项目从课程本地演示升级为可部署原型。

### 题目核心
- [x] 将 demo token 登录升级为 JWT 登录
- [ ] 角色权限 RBAC
- [x] Owner 只能管理自己的任务
- [x] Labeler 只能访问自己领取的任务
- [x] Reviewer 只能审核被分配或可见的任务

### 工程化增强
- [x] 增加接口权限测试
- [ ] 编写 Docker Compose 部署方案
- [ ] 增加 Nginx 前后端统一入口
- [ ] 增加生产环境配置说明
- [ ] 准备最小云演示环境（1 台轻量云服务器 + Docker Compose，优先保证黄金路径可访问）
- [ ] 增加健康检查接口和基础日志
- [ ] 若继续保留 submission 级 reviewer assign 接口，需明确其与 task 级 reviewer 分配的边界，避免与本轮权限口径冲突

---

